require "./08_chat"

test "the assistant remembers the conversation" do
  mock :fast, replies: ["Hi Ada!", "You are Ada."]
  chat = Conversation.new(model: :fast)
  chat.say("I am Ada")
  assert_equal "You are Ada.", chat.say("Who am I?").trust!
  assert_equal 4, chat.history.size
end
