[package]
name = "grenat_mcp"
description = "The Model Context Protocol: a client of MCP servers (stdio or HTTP), and the server side"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
serde_json.workspace = true
ureq.workspace = true

[dev-dependencies]
grenat_mcp = { path = ".", features = ["fake"] }

[[test]]
name = "client"
harness = false

[features]
# A fake MCP server, for the tests of this crate and of its users.
fake = []
